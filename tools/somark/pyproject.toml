[project]
name = "somark"
version = "0.0.1"
description = "Somark document extraction plugin"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dify-plugin>=0.3.0,<0.6.0",
    "requests",
]

[dependency-groups]
dev = []
